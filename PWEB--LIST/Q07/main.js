let base = parseInt(prompt("digite o valor da base: "));
let altura = parseInt(prompt("digite a altura do retângulo: "));

area = base * altura;
perimetro = (2*altura) + (2*base);

document.write("a area do retangulo é de " + area);
document.write(" e o perimetro do triangulo "+  perimetro);